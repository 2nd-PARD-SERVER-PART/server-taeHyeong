<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Info Result</title>
    <!-- 부트스트랩 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: darkseagreen;
            color: darkseagreen;
        }
        h1 {
            text-align: center;
            padding-top: 20px;
            color: white;
        }
        p {
            font-size: 18px;
            margin: 10px;
        }
        .result-info {
            background-color: white;
            padding: 10px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);
        }
    </style>
</head>
<body>
<h1>My Info Result</h1>
<div class="result-info">
    <p><strong>Major:</strong> {{myInfo.getMajor}}</p>
    <p><strong>Grade:</strong> {{myInfo.getGrade}}</p>
    <p><strong>Age:</strong> {{myInfo.getAge}}</p>
    <p><strong>Hobby:</strong> {{myInfo.getHobby}}</p>
    <p><strong>Name:</strong> {{myInfo.getName}}</p>
    <p><strong>Hometown:</strong> {{myInfo.getHometown}}</p>
    <p><strong>Introduction:</strong> {{myInfo.getIntroduction}}</p>
</div>
</body>
</html>